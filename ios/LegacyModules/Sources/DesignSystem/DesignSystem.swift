import SwiftUI

/// Legacy's design language: warm, nostalgic, dark-first.
///
/// Token groups:
/// - `LegacyColor`    — semantic color palette
/// - `LegacyFont`     — type scale (rounded display, default body)
/// - `LegacySpacing`  — 4pt-based spacing scale
/// - `LegacyRadius`   — corner radii
/// - Warmth cue       — `WarmthCueOverlay` / `WarmthLevel` (non-directional, DEC-15)
/// - Button styles    — `PrimaryButtonStyle`, `SecondaryButtonStyle`
public enum DesignSystem {
    public static let version = "0.1.0"
}
